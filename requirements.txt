# Data Engine — real HTTP calls to OANDA's REST API
requests

# Journal Engine, Currency Strength, Technical, Voting, Risk, ML, Analytics —
# all stdlib only (sqlite3, json, dataclasses, math). No dependencies needed.

# --- Not currently used; most indicator/ML math ended up hand-implemented
# in pure stdlib instead, but listed here in case you want to swap to
# these later ---
# oandapyV20      # alternative OANDA client wrapper (requests is used directly instead)
# pandas          # only needed if candle handling grows beyond plain dicts
# ta              # alternative to the hand-rolled indicators.py
# river           # alternative to the hand-rolled OnlineTradeModel
